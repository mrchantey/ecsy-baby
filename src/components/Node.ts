import { Node as BabylonNode } from 'babylonjs';
import { Component, ComponentSchema, Types } from 'ecsy';

export class Node extends Component<Node> {
	// value: BabylonNode

	// static schema: ComponentSchema = {
	// 	value: { type: Types.Ref }
	// }
}